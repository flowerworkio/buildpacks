#!/usr/bin/env bash

set -euo pipefail

cd "$(dirname "$0")/.."
source bin/functions.sh

package_stacks
package_builder
package_buildpacks

if [ "$BUILDKITE_BRANCH" == "main" ]; then
  ./bin/publish
fi
